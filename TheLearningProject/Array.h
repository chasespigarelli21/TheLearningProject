

// Method declarations


    // Takes in an array and array length and sorts the array
	void DisplayArray(int* someArray, int someLength);

	// Takes in an array and array length and returns the sorted array
	int* SortArray(int someArray[], int length);

	// Takes in an array and displays the address of the array
	void DisplayArrayAddress(int* someArray);

	// Takes in an array and array size and displays the address of each integer in the array
	void DisplayArrayIntegerAddresses(int* someArray, int someSize);

	// Takes in an array length and array and gets digits from the user to put into the array and return the array pointer
	int* GetDigitsForArray(int length, int* someArray);

	// Prompt the user for their desired array length
	int PromptArrayLength();